server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SrinathRayabarapu/springboot-config-server
          clone-on-start: true
          default-label: main

logging:
  level:
    ROOT: INFO
    org.springframework.cloud.config: DEBUG